// algorithm which receives the *time of day* in three different numbers, 
// the hour, the minutes and the seconds and returns the *number of seconds* since
// midnight.

function daySecond(h,m,s){
    return s + m*60 + h*60*60;
    r
}

